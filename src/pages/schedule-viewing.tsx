import { NavBar } from "@/components/navbar";
import { ScheduleForm } from "@/components/schedule-form";

export const ScheduleViewingPage = () => {
  return (
    <div>
      <NavBar />
      <ScheduleForm />
    </div>
  );
};
